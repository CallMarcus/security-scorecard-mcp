{
  "name": "security-scorecard-mcp",
  "version": "0.1.0",
  "description": "Compiled MCP server for the SecurityScorecard API",
  "main": "build/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node build/index.js",
    "test": "NODE_ENV=test node --loader ts-node/esm --test tests/*.test.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.6.0",
    "@xenova/transformers": "^2.17.2"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@types/node": "^24.1.0",
    "typescript": "^5.8.3",
    "ts-node": "^10.9.2"
  }
}
