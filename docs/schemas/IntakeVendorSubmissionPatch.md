# IntakeVendorSubmissionPatch

```json
{
  "type": "object",
  "description": "Payload for updating an intake vendor form submission.",
  "properties": {
    "review_state": {
      "type": "string",
      "description": "New review state for the submission (e.g. pending, approved, rejected)."
    }
  }
}
```
