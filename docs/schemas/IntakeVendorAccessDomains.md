# IntakeVendorAccessDomains

```json
{
  "type": "object",
  "description": "List of email domains with access to the current intake vendor form.",
  "properties": {
    "domains": {
      "type": "array",
      "description": "Email domains permitted to access the form.",
      "items": {
        "type": "string"
      }
    }
  }
}
```
